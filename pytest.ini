[pytest]
DJANGO_SETTINGS_MODULE = config.test_settings
python_files = tests/test_*.py
testpaths = forum/tests
markers =
    slow: tests that are slow (e.g. integration)
    smoke: quick sanity checks
